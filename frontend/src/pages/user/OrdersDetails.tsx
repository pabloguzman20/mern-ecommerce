import React from "react";

const OrderDetails: React.FunctionComponent = (): JSX.Element => {
    return (
        <div>
            <h1>Order Details</h1>
        </div>
    );
};

export default OrderDetails;